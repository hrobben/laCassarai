{% extends 'base.html.twig' %}

{% block title %}Edit Reservatie{% endblock %}

{% block body %}
    <h1>Edit Reservatie</h1>

    {{ include('reservatie/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reservatie_index') }}">back to list</a>

    {{ include('reservatie/_delete_form.html.twig') }}
{% endblock %}
