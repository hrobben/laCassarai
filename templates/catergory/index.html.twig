{% extends 'base.html.twig' %}

{% block title %}Catergory index{% endblock %}

{% block body %}
    <h1>Catergory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Omschrijving</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for catergory in catergories %}
            <tr>
                <td>{{ catergory.id }}</td>
                <td>{{ catergory.omschrijving }}</td>
                <td>
                    <a href="{{ path('catergory_show', {'id': catergory.id}) }}">show</a>
                    <a href="{{ path('catergory_edit', {'id': catergory.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('catergory_new') }}">Create new</a>
{% endblock %}
