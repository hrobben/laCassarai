{% extends 'base.html.twig' %}

{% block title %}Tafel{% endblock %}

{% block body %}
    <h1>Tafel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tafel.id }}</td>
            </tr>
            <tr>
                <th>Personen</th>
                <td>{{ tafel.personen }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tafel_index') }}">back to list</a>

    <a href="{{ path('tafel_edit', {'id': tafel.id}) }}">edit</a>

    {{ include('tafel/_delete_form.html.twig') }}
{% endblock %}
